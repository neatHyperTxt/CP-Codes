#include<iostream>
#include<vector>
#define ll long long int

using namespace std;

vector<ll> factorization(ll n)     // O(sqrt(n)) Time Complexity
{
	vector<ll>factors;
	for(ll i=2;i*i<=n;i++)
	{
		while(n%i==0)
		{
			factors.push_back(i);
			n/=i;
		}
	}
	if(n>1)
	{
		factors.push_back(n);
	}
	return factors;
}


vector<ll> wheelFactoriation(ll n) // faster than normal Factorization
{
	vector<ll>factors;
	while(n%2==0)
	{
		factors.push_back(2);
		n/=2;
	}
	for(ll i=3;i*i<=n;i+=2)
	{
		while(n%i==0)
		{
			factors.push_back(i);
			n/=i;
		}
	}
	if(n>1)
	{
		factors.push_back(n);
	}
	return factors;
}
int main()
{
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	ll t;
	cout<<"Enter the Testcases"<<endl;
	cin>>t;
	while(t--)
	{
		ll n;
		cout<<"Enter the Number"<<endl;
		cin>>n;
		vector<ll>v = wheelFactoriation(n);
		for(ll i=0;i<v.size();i++)
		{
			cout<<v[i]<<" ";
		}
		cout<<endl;
	}
	return 0;
}
